<h1>New researcher</h1>

<%= form_for(@researcher) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => 'researcher', :locals => {:f => f} %>
  
  <%= f.submit "Create" %>
<% end %>

<%= link_to 'Back', researchers_path %>
