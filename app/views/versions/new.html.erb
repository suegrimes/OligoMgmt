<h1>New version</h1>

<%= form_for(@version) do |f| %>
<% @version.errors.full_messages.each do |msg| %>
    <p>Error: <%= msg %></p>
<% end %>

  <%= render :partial => 'version', :locals => {:f => f} %>
  
  <%= f.submit "Create" %>
<% end %>

<%= link_to 'Back', versions_path %>
